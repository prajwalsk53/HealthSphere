name: Deploy to IONOS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SFTP
        uses: SamKirkland/FTP-Deploy-Action@v4
        with:
          server: access-5020406432.webspace-host.com
          username: su852728
          password: ${{ secrets.SFTP_PASSWORD }}
          protocol: sftp
          port: 22
          local-dir: ./
          server-dir: /HealthSphere/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            config/mail.php
            logs/**
            sql/**
            *.sql
